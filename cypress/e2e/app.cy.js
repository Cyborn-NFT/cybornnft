describe('Navigation', () => {
    it('should navigate to the about page', () => {
            cy.visit('http://localhost:3000/')
        })
        // it('Should have a register page', ()=>{
        //     cy.visit('http://localhost:3000/register')
        // })
})